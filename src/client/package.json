{
  "name": "dt-sdk",
  "version": "1.0.0",
  "license": "MIT",
  "author": "DataTower",
  "description": "DataTower SDK",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json && node dist/build.js",
    "dev": "tsc -p tsconfig.build.json && node dist/build.dev.js"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "tsup": "^8.0.2",
    "typescript": "^5.4.3"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
